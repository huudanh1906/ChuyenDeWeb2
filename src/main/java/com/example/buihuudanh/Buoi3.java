// Trigger dùng để thực thi một nhóm câu lệnh SQL

// Trigger thực thi một cách tự động khi một trong ba câu lệnh Insert, Update,
// Delete
// làm thay đổi dữ liệu trên bảng có chứa trigger.

// Trigger thường được sử dụng để kiểm tra ràng buộc (check constraints) trên
// nhiều quan hệ (nhiều bảng/table)
// hoặc trên nhiều dòng (nhiều record) của bảng.

// Stored Procedure là gì?

// Stored Procedure là một tập hợp các câu lệnh SQL được lưu trữ trong cơ sở dữ
// liệu và có thể được gọi để thực thi nhiều lần.

// Chỉ được thực thi khi được gọi thủ công bằng lệnh CALL hoặc EXEC.

// Thường dùng để đóng gói logic xử lý phức tạp, tái sử dụng mã SQL, tối ưu hiệu
// suất.